---
phase: 02-core-marketing-site
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/pricing/page.tsx
  - src/components/ui/PricingCard.tsx
  - src/components/ui/PricingToggle.tsx
  - src/components/ui/FAQ.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Button.tsx
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05

must_haves:
  truths:
    - "The pricing page at /pricing shows all 3 plan tiers (Starter, Professional, Agency) in a 3-column grid"
    - "A monthly/annual toggle switches displayed prices — annual shows annual_price_display, monthly shows monthly_price_display"
    - "Annual toggle shows savings percentage (e.g., 'Save 17%') per plan.annual_savings_percent from plans.json"
    - "Each pricing card shows: plan name, tagline, price, features list, CTA button, and badge if applicable"
    - "Pricing data is NEVER hardcoded — always sourced from getAllPlans() reading plans.json"
    - "An FAQ accordion section appears below the pricing cards"
    - "FAQ items expand/collapse on click"
  artifacts:
    - path: "src/app/pricing/page.tsx"
      provides: "Pricing page — server component composing PricingToggle + FAQ"
      exports: ["default PricingPage", "metadata"]
    - path: "src/components/ui/PricingToggle.tsx"
      provides: "Monthly/annual toggle + PricingCard grid — 'use client'"
      exports: ["default PricingToggle"]
    - path: "src/components/ui/PricingCard.tsx"
      provides: "Individual plan card reading Plan object prop"
      exports: ["default PricingCard"]
    - path: "src/components/ui/FAQ.tsx"
      provides: "Accordion FAQ — 'use client'"
      exports: ["default FAQ"]
    - path: "src/components/ui/Badge.tsx"
      provides: "Reusable badge component for 'Most Popular' etc."
      exports: ["default Badge"]
    - path: "src/components/ui/Button.tsx"
      provides: "Reusable button (primary/secondary/outline variants)"
      exports: ["default Button"]
  key_links:
    - from: "src/app/pricing/page.tsx"
      to: "src/components/ui/PricingToggle.tsx"
      via: "import PricingToggle, render below page heading — toggle calls getAllPlans() internally"
    - from: "src/components/ui/PricingToggle.tsx"
      to: "src/lib/plans.ts"
      via: "import getAllPlans, call server-side or in component, pass plans to PricingCard"
    - from: "src/components/ui/PricingCard.tsx"
      to: "plan.cta_url"
      via: "CTA button href reads plan.cta_url (Agency goes to /contact?plan=agency)"
---

<objective>
Build the pricing page with all 3 plan cards, a monthly/annual toggle that switches prices, and an FAQ accordion. Reusable Button and Badge UI primitives are also created here for use across other pages.

Purpose: The pricing page is the conversion destination for paid plans. Prices must always come from plans.json to ensure single source of truth.
Output: /pricing page, PricingToggle (client), PricingCard, FAQ (client), Badge, Button components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-marketing-site/02-01-SUMMARY.md
@.planning/phases/02-core-marketing-site/02-02-SUMMARY.md

<interfaces>
<!-- From src/lib/plans.ts — Plan type for PricingCard prop -->
```typescript
export interface Plan {
  name: string;              // "Starter" | "Professional" | "Agency"
  tagline: string;           // "For solo and freelance recruiters"
  monthly_price_display: string;  // "$29"
  annual_price_display: string;   // "$24"
  annual_savings_percent: number; // 17
  features: string[];        // array of feature strings
  features_intro?: string;   // "Everything in Starter, plus:" — only on Professional and Agency
  cta_text: string;          // "Start Free Trial" | "Contact Us"
  cta_url: string;           // "https://app.kineticrecruiter.com/register?plan=starter" | "/contact?plan=agency"
  badge: string | null;      // "Most Popular" | null
  sort_order: number;
}
export function getAllPlans(): Plan[]  // returns [starter, professional, agency] sorted by sort_order
```

<!-- From src/components/sections/CTASection.tsx (Plan 02) -->
```typescript
export default function CTASection(props?: { headline?: string; subheadline?: string; ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button, Badge, PricingCard UI components</name>
  <files>
    src/components/ui/Button.tsx
    src/components/ui/Badge.tsx
    src/components/ui/PricingCard.tsx
    src/components/ui/.gitkeep
  </files>

  <read_first>
    - package/kinetic-recruiter/src/pages/PricingPage.tsx — FULL FILE: pricing card layout, badge position, feature list styling, CTA button styles
    - package/kinetic-recruiter/src/components/FAQ.tsx — pricing page FAQ items (6 questions and answers)
    - src/app/globals.css — Tailwind v4 CSS var names (use inline hex: #0d8488 teal, #1a2332 navy)
  </read_first>

  <action>
    Create src/components/ui/Button.tsx (server component):
    - Props: variant: 'primary' | 'secondary' | 'outline', size?: 'sm' | 'md' | 'lg', href?: string, children, className?, onClick?, type?
    - If href provided: render as Link from 'next/link'; otherwise render as <button>
    - Primary: bg-[#0d8488] hover:bg-[#0a6b6e] text-white font-semibold rounded-lg px-5 py-2.5 transition-colors
    - Secondary: bg-[#1a2332] text-white hover:bg-[#2a3a4f] font-semibold rounded-lg px-5 py-2.5 transition-colors
    - Outline: border-2 border-[#0d8488] text-[#0d8488] hover:bg-[#E8F5F5] font-semibold rounded-lg px-5 py-2.5 transition-colors
    - Sizes: sm (px-3 py-1.5 text-sm), md (px-5 py-2.5), lg (px-6 py-3 text-lg)
    - NOTE: onClick and type require 'use client' — if onClick prop is passed, component needs 'use client'. Make a server-only version that accepts href only, OR mark it 'use client' since it's a leaf UI component used in both contexts.
    - Decision: mark Button as 'use client' — it's a leaf component used everywhere, client overhead is minimal

    Create src/components/ui/Badge.tsx (server component):
    - Props: children, variant?: 'teal' | 'amber' | 'navy', className?
    - Teal (default): bg-[#0d8488] text-white text-xs font-semibold px-3 py-1 rounded-full
    - Amber: bg-[#E8A838] text-white text-xs font-semibold px-3 py-1 rounded-full
    - Navy: bg-[#1a2332] text-white text-xs font-semibold px-3 py-1 rounded-full

    Create src/components/ui/PricingCard.tsx (server component):
    - Props: plan: Plan, isAnnual: boolean
    - Display price: isAnnual ? plan.annual_price_display : plan.monthly_price_display
    - Show annual savings when isAnnual: "Save {plan.annual_savings_percent}%" as a small badge
    - If plan.badge is non-null: render Badge component with badge text (e.g., "Most Popular")
    - Professional plan card: add border-2 border-[#0d8488] and scale-105 for visual prominence
    - Features list: if plan.features_intro exists, show as italic intro text above the list
    - Each feature: checkmark icon (lucide-react Check w-4 h-4 text-[#0d8488]) + feature text
    - CTA button: Link href={plan.cta_url} with plan.cta_text as label
    - Agency plan CTA goes to /contact?plan=agency — verify plan.cta_url from plans.json
    - Card: bg-white rounded-2xl border border-gray-200 p-8 flex flex-col shadow-sm
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      grep -l "variant" src/components/ui/Button.tsx && \
      grep -l "isAnnual" src/components/ui/PricingCard.tsx && \
      grep -l "plan.badge" src/components/ui/PricingCard.tsx && \
      npm run build 2>&1 | grep -E "error TS|✓ Compiled" | head -5
    </automated>
  </verify>

  <acceptance_criteria>
    - src/components/ui/Button.tsx exists with variant prop (grep "variant" returns match)
    - src/components/ui/Badge.tsx exists
    - src/components/ui/PricingCard.tsx exists with isAnnual prop and plan.badge reference
    - grep "getAllPlans\|plan\.badge\|plan\.features" src/components/ui/PricingCard.tsx returns matches
    - No hardcoded plan names or prices anywhere in PricingCard.tsx — all values come from plan prop
    - npm run build exits 0
  </acceptance_criteria>

  <done>Button (3 variants), Badge (3 variants), and PricingCard (reads Plan prop, shows isAnnual toggle price, renders badge and features list) created.</done>
</task>

<task type="auto">
  <name>Task 2: Build PricingToggle, FAQ accordion, and pricing page</name>
  <files>
    src/components/ui/PricingToggle.tsx
    src/components/ui/FAQ.tsx
    src/app/pricing/page.tsx
  </files>

  <read_first>
    - package/kinetic-recruiter/src/pages/PricingPage.tsx — FULL FILE: page heading, subheading, toggle button design, overall page layout
    - package/kinetic-recruiter/src/components/FAQ.tsx — FULL FILE: 6 FAQ questions and answers (copy exact text)
    - src/components/ui/PricingCard.tsx — just created above — prop interface to confirm
  </read_first>

  <action>
    Create src/components/ui/PricingToggle.tsx ('use client'):
    - 'use client' directive at top
    - import { useState } from 'react'
    - import getAllPlans from '@/lib/plans'
    - import PricingCard from './PricingCard'
    - State: const [isAnnual, setIsAnnual] = useState(false)
    - const plans = getAllPlans() — call at component render (runs client-side; getAllPlans is sync JSON read)
    - Toggle UI: two-button pill switcher:
      Monthly button: onClick={() => setIsAnnual(false)}, active state bg-[#0d8488] text-white, inactive bg-gray-100 text-gray-600
      Annual button: onClick={() => setIsAnnual(true)}, same active/inactive styling
      Wrapper: inline-flex rounded-full border border-gray-200 p-1
      Add "Save 17%" badge next to Annual button when inactive to prompt switching
    - Cards grid: <div className="grid md:grid-cols-3 gap-8 mt-12">
      {plans.map(plan => <PricingCard key={plan.name} plan={plan} isAnnual={isAnnual} />)}
    - NOTE: getAllPlans() reads from src/lib/plans.json. In a client component this works because Next.js bundles JSON imports. Confirm this works with npm run build.

    Create src/components/ui/FAQ.tsx ('use client'):
    - 'use client' directive at top
    - import { useState } from 'react'
    - import { ChevronDown } from 'lucide-react'
    - State: const [openIndex, setOpenIndex] = useState<number | null>(null)
    - FAQ items hardcoded from MiniMax FAQ.tsx source — COPY EXACT text for all 6 questions and answers:
      1. "What types of teams is KineticRecruiter built for?" — copy answer from MiniMax
      2. "What AI capabilities are included?" — copy answer from MiniMax
      3. "How does the free trial work?" — copy answer from MiniMax
      4. "Can I import my existing candidates?" — copy answer from MiniMax
      5. "How does billing work?" — copy answer from MiniMax
      6. "Is there a mobile app?" — copy answer from MiniMax
    - Accordion: each item is a div with a button (toggles openIndex) and a conditionally rendered answer div
    - Match MiniMax accordion styling: border border-gray-200 rounded-xl, answer fade-in (CSS transition or simple conditional render)
    - ChevronDown rotates 180deg when open: className={`transition-transform ${openIndex === index ? 'rotate-180' : ''}`}

    Create src/app/pricing/page.tsx (server component):
    - Export metadata: generatePageMetadata({ title: 'Pricing', description: 'Simple, transparent pricing. All AI features included. No add-ons. No hidden costs. Start your 7-day free trial.', path: '/pricing' })
    - Page heading: "Simple, transparent pricing" (or exact heading from MiniMax PricingPage.tsx — verify)
    - Subheading: "Every AI feature included. No add-ons. No hidden costs." (or exact from MiniMax)
    - Render <PricingToggle /> below heading
    - Render <FAQ /> below pricing cards
    - Render <CTASection /> at bottom of page (import from sections)
    - Wrap everything in <main className="min-h-screen bg-white">
    - Section max-width: <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24">
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      grep "'use client'" src/components/ui/PricingToggle.tsx && \
      grep "'use client'" src/components/ui/FAQ.tsx && \
      grep "generatePageMetadata" src/app/pricing/page.tsx && \
      grep "PricingToggle" src/app/pricing/page.tsx && \
      grep "FAQ" src/app/pricing/page.tsx && \
      npm run build 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "'use client'" src/components/ui/PricingToggle.tsx returns a match
    - grep "'use client'" src/components/ui/FAQ.tsx returns a match
    - grep "getAllPlans" src/components/ui/PricingToggle.tsx returns a match (pricing never hardcoded — per PRICE-04)
    - FAQ.tsx contains all 6 question strings (grep one: "What types of teams" returns match)
    - src/app/pricing/page.tsx contains generatePageMetadata, PricingToggle, FAQ, CTASection
    - npm run build exits 0 with no TypeScript errors
    - /pricing route included in Next.js build output
  </acceptance_criteria>

  <done>PricingToggle ('use client' with useState for isAnnual), FAQ accordion ('use client'), and /pricing page assembled with full SSR metadata and both client components embedded.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `npm run dev` — visit localhost:3000/pricing
3. Click Monthly/Annual toggle — prices switch between monthly_price_display and annual_price_display
4. Verify Professional plan shows "Most Popular" badge
5. Agency CTA links to /contact?plan=agency
6. FAQ items expand/collapse on click
7. view-source of /pricing shows plan names and headings in HTML (not blank)
</verification>

<success_criteria>
- All 3 pricing tiers display from getAllPlans() with no hardcoded prices
- Monthly/annual toggle switches prices correctly
- Agency plan CTA links to /contact?plan=agency (not a registration URL)
- FAQ accordion has all 6 questions, expands/collapses
- /pricing page has unique title and meta description
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-site/02-03-SUMMARY.md`
</output>
