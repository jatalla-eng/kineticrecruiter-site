---
phase: 02-core-marketing-site
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/components/ui/FeatureSection.tsx
  - src/app/features/ai-candidate-intelligence/page.tsx
  - src/app/features/candidate-intake/page.tsx
  - src/app/features/agency-workflow/page.tsx
  - src/app/features/team-platform/page.tsx
  - src/app/solutions/recruitment-agencies/page.tsx
  - src/app/solutions/in-house-teams/page.tsx
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
  - SOL-01
  - SOL-02
  - SOL-03

must_haves:
  truths:
    - "Four feature pages exist at /features/ai-candidate-intelligence, /features/candidate-intake, /features/agency-workflow, /features/team-platform"
    - "Two solution pages exist at /solutions/recruitment-agencies, /solutions/in-house-teams"
    - "Each feature page has alternating text+image sections using the reusable FeatureSection component"
    - "Each page has a hero section with page-specific headline, tailored messaging, and a CTA"
    - "Each page has unique title and meta description via generatePageMetadata()"
    - "Each page ends with the reusable CTASection component"
    - "Solution pages have agency-specific and corporate HR-specific messaging respectively"
  artifacts:
    - path: "src/components/ui/FeatureSection.tsx"
      provides: "Alternating text+image layout component — server component"
      exports: ["default FeatureSection"]
    - path: "src/app/features/ai-candidate-intelligence/page.tsx"
      provides: "AI Candidate Intelligence feature page"
    - path: "src/app/features/candidate-intake/page.tsx"
      provides: "Candidate Intake feature page"
    - path: "src/app/features/agency-workflow/page.tsx"
      provides: "Agency Workflow feature page"
    - path: "src/app/features/team-platform/page.tsx"
      provides: "Team Platform feature page"
    - path: "src/app/solutions/recruitment-agencies/page.tsx"
      provides: "Recruitment Agencies solution page"
    - path: "src/app/solutions/in-house-teams/page.tsx"
      provides: "In-House Teams solution page"
  key_links:
    - from: "all feature pages"
      to: "src/components/ui/FeatureSection.tsx"
      via: "import FeatureSection, pass headline/body/bullets/illustration/reverse props"
    - from: "all feature and solution pages"
      to: "src/components/sections/CTASection.tsx"
      via: "import CTASection and render at bottom of each page"
---

<objective>
Build the reusable FeatureSection component and all 4 feature pages + 2 solution pages. Every page is server-rendered with unique SEO metadata. Content and copy come directly from MiniMax source pages.

Purpose: These pages are key SEO and conversion pages for search queries like "recruitment agency ATS" and "AI candidate matching software". Full SSR is critical.
Output: FeatureSection component + 6 page routes fully populated with MiniMax content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-marketing-site/02-01-SUMMARY.md
@.planning/phases/02-core-marketing-site/02-02-SUMMARY.md

<interfaces>
<!-- From src/lib/metadata.ts (Phase 1) -->
```typescript
export function generatePageMetadata(opts: {
  title: string;
  description: string;
  path: string;
  image?: string;
}): Metadata
```

<!-- FeatureSection component to create — contract to implement -->
```typescript
interface FeatureSectionProps {
  headline: string;
  body: string;
  bullets: string[];
  imageSrc?: string;        // path for next/image, e.g. "/images/recruiter-1.png"
  imageAlt?: string;
  reverse?: boolean;        // true = image on left, text on right
  imageWidth?: number;      // default 600
  imageHeight?: number;     // default 450
}
```

<!-- CTASection from Plan 02 — accepts optional props -->
```typescript
export default function CTASection(props?: {
  headline?: string;
  subheadline?: string;
  primaryCTA?: { text: string; href: string };
  secondaryCTA?: { text: string; href: string };
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeatureSection component and all 4 feature pages</name>
  <files>
    src/components/ui/FeatureSection.tsx
    src/app/features/ai-candidate-intelligence/page.tsx
    src/app/features/candidate-intake/page.tsx
    src/app/features/agency-workflow/page.tsx
    src/app/features/team-platform/page.tsx
  </files>

  <read_first>
    - package/kinetic-recruiter/src/pages/FeatureAI.tsx — FULL FILE: AI Candidate Intelligence page sections, headlines, body text, bullet points, image references
    - package/kinetic-recruiter/src/pages/FeatureIntake.tsx — FULL FILE: Candidate Intake page content
    - package/kinetic-recruiter/src/pages/FeatureAgency.tsx — FULL FILE: Agency Workflow page content
    - package/kinetic-recruiter/src/pages/FeatureTeam.tsx — FULL FILE: Team Platform page content
    - package/kinetic-recruiter/src/components/FeatureIllustration1.tsx — note illustration structure (inline SVG)
    - package/kinetic-recruiter/src/components/FeatureIllustration2.tsx — note illustration structure
  </read_first>

  <action>
    Create src/components/ui/FeatureSection.tsx (server component):
    - Implement FeatureSectionProps interface as defined above
    - Layout: <div className="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
    - Text side: headline (h3 text-2xl md:text-3xl font-bold text-[#1a2332]), body paragraph (text-gray-600 text-lg), bullet list with teal checkmarks
    - Image side: next/image with imageSrc, imageAlt, imageWidth, imageHeight, rounded-xl shadow-md
    - When reverse=true: image side gets lg:order-first (or use order-1/order-2 on grid children)
    - Checkmark icon: use lucide-react Check (w-5 h-5 text-[#0d8488]) in each bullet li
    - Section wrapper: <section className="py-16 md:py-24"> with max-w-7xl mx-auto px-4 sm:px-6 lg:px-8

    For FeatureIllustrations from MiniMax:
    - If MiniMax feature pages use <FeatureIllustrationN /> components (inline SVG), either:
      a) Use the available photos from public/images/ instead (recruiter-1.png through team-meeting.png)
      b) If the inline SVGs are critical to design fidelity, copy them as src/components/illustrations/FeatureIllustration1.tsx through 6.tsx
    - Use next/image with actual photos if illustrations are complex; note deviation in task completion comment

    Create src/app/features/ai-candidate-intelligence/page.tsx:
    - Read FeatureAI.tsx for EXACT content — copy headlines, body, bullets for each section
    - metadata: generatePageMetadata({ title: 'AI Candidate Intelligence | KineticRecruiter', description: 'Semantic search and AI scoring that understands context. Find the right candidates in seconds, not hours.', path: '/features/ai-candidate-intelligence' })
    - Page hero: h1 with page headline, subheadline paragraph, CTA button linking to /pricing
    - 2-3 FeatureSection components (alternating reverse prop: false, true, false)
    - CTASection at bottom
    - Image assignments: use recruiter-1.png, recruiter-2.png, team-meeting.png as appropriate

    Create src/app/features/candidate-intake/page.tsx:
    - Read FeatureIntake.tsx for content — copy all section text exactly
    - metadata: generatePageMetadata({ title: 'Candidate Intake | KineticRecruiter', description: 'Bulk resume uploads, inbound email processing, and LinkedIn Chrome extension. Fill your pipeline automatically.', path: '/features/candidate-intake' })
    - Same structure: hero + FeatureSection(s) + CTASection

    Create src/app/features/agency-workflow/page.tsx:
    - Read FeatureAgency.tsx for content
    - metadata: generatePageMetadata({ title: 'Agency Workflow | KineticRecruiter', description: 'Client CRM, job shortlist Kanban, and AI submission emails built for recruitment agencies.', path: '/features/agency-workflow' })
    - Same structure: hero + FeatureSection(s) + CTASection

    Create src/app/features/team-platform/page.tsx:
    - Read FeatureTeam.tsx for content
    - metadata: generatePageMetadata({ title: 'Team & Platform | KineticRecruiter', description: 'Multi-seat team access, API integrations, and role-based permissions for growing agencies.', path: '/features/team-platform' })
    - Same structure: hero + FeatureSection(s) + CTASection

    All 4 pages: server components (no 'use client'), import FeatureSection and CTASection.
    All images: use next/image with descriptive alt text (per SEO-05 / D-17).
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      ls src/app/features/ai-candidate-intelligence/page.tsx && \
      ls src/app/features/candidate-intake/page.tsx && \
      ls src/app/features/agency-workflow/page.tsx && \
      ls src/app/features/team-platform/page.tsx && \
      grep -l "FeatureSection" src/app/features/ai-candidate-intelligence/page.tsx && \
      grep -l "generatePageMetadata" src/app/features/ai-candidate-intelligence/page.tsx && \
      npm run build 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - All 4 feature page.tsx files exist in their respective route directories
    - Each feature page contains generatePageMetadata with unique title and path
    - Each feature page imports and renders FeatureSection
    - Each feature page imports and renders CTASection
    - No feature page contains 'use client' (all server components)
    - grep "next/image" src/components/ui/FeatureSection.tsx returns a match
    - npm run build exits 0 and all 4 feature routes appear in build output
  </acceptance_criteria>

  <done>FeatureSection component with alternating text+image layout; all 4 feature pages populated with MiniMax content, unique metadata, and reusable CTASection.</done>
</task>

<task type="auto">
  <name>Task 2: Build both solution pages</name>
  <files>
    src/app/solutions/recruitment-agencies/page.tsx
    src/app/solutions/in-house-teams/page.tsx
  </files>

  <read_first>
    - package/kinetic-recruiter/src/pages/SolutionsAgencies.tsx — FULL FILE: agency-specific hero, pain points, features list, testimonial/social proof if present, CTA
    - package/kinetic-recruiter/src/pages/SolutionsInHouse.tsx — FULL FILE: corporate HR-specific hero, pain points, features, CTA
    - src/components/ui/FeatureSection.tsx — just created, confirm prop interface
    - src/components/sections/CTASection.tsx — confirm default props
  </read_first>

  <action>
    Create src/app/solutions/recruitment-agencies/page.tsx (server component):
    - Read SolutionsAgencies.tsx for EXACT content — agency-specific messaging
    - metadata: generatePageMetadata({
        title: 'For Recruitment Agencies | KineticRecruiter',
        description: 'ATS built for recruitment agencies. Manage candidates across multiple clients, track placements, and close more roles with AI-powered matching.',
        path: '/solutions/recruitment-agencies'
      })
    - Hero section: h1 with agency-specific headline from MiniMax, subheadline, CTA "Start Free Trial" → https://app.kineticrecruiter.com/register
    - Pain points section: "The challenges agencies face" — list the agency-specific pain points from SolutionsAgencies.tsx (copy exact text)
    - Features section: use FeatureSection component or a custom features grid showing how KineticRecruiter solves agency challenges (content from MiniMax)
    - Social proof / quote if present in MiniMax source
    - CTASection at bottom

    Create src/app/solutions/in-house-teams/page.tsx (server component):
    - Read SolutionsInHouse.tsx for EXACT content — corporate HR/in-house team messaging
    - metadata: generatePageMetadata({
        title: 'For In-House Talent Teams | KineticRecruiter',
        description: 'ATS for corporate HR and talent acquisition teams. Streamline internal hiring with AI candidate scoring, structured intake, and team collaboration.',
        path: '/solutions/in-house-teams'
      })
    - Hero: corporate/in-house specific headline and subheadline from MiniMax (copy exact text)
    - Pain points and features sections using FeatureSection or equivalent layout
    - CTASection at bottom

    Both pages:
    - Server components (no 'use client')
    - All images via next/image with descriptive alt text
    - Use team-meeting.png or video-call.png for in-house page, recruiter-success.png or recruiter-3.png for agencies page
    - Import FeatureSection for alternating layout sections
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      ls src/app/solutions/recruitment-agencies/page.tsx && \
      ls src/app/solutions/in-house-teams/page.tsx && \
      grep "generatePageMetadata" src/app/solutions/recruitment-agencies/page.tsx && \
      grep "generatePageMetadata" src/app/solutions/in-house-teams/page.tsx && \
      npm run build 2>&1 | grep -E "error TS|✓ Compiled|Route" | head -20
    </automated>
  </verify>

  <acceptance_criteria>
    - src/app/solutions/recruitment-agencies/page.tsx exists with agency-specific h1 text (not generic)
    - src/app/solutions/in-house-teams/page.tsx exists with in-house/corporate specific h1 text (not generic)
    - Both pages have generatePageMetadata with unique title/description
    - Neither page contains 'use client'
    - Both pages render CTASection
    - npm run build exits 0 and both solution routes appear in build output
    - grep "recruitment.agenc" src/app/solutions/recruitment-agencies/page.tsx returns a match (agency-specific content)
  </acceptance_criteria>

  <done>Both solution pages built as server components with tailored messaging: agency-specific pain points + features for /solutions/recruitment-agencies and corporate HR messaging for /solutions/in-house-teams.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0 and all 6 routes visible in build output
2. Visit each page in dev mode and verify content is populated (not placeholder)
3. view-source of /features/ai-candidate-intelligence — see h1 headline text in HTML
4. view-source of /solutions/recruitment-agencies — see agency-specific headline
5. CTASection renders at bottom of all 6 pages
</verification>

<success_criteria>
- 4 feature pages and 2 solution pages all exist and build cleanly
- Each page has unique metadata via generatePageMetadata()
- All pages are server components with full SSR content
- FeatureSection component reused across feature pages with alternating layout
- CTASection renders on all 6 pages
- Content matches MiniMax source — no placeholder text
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-site/02-04-SUMMARY.md`
</output>
