---
phase: 02-core-marketing-site
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/contact/page.tsx
  - src/app/sitemap.ts
  - public/robots.txt
  - public/og-default.jpg
autonomous: true
requirements:
  - CONTACT-01
  - CONTACT-02
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "The contact page at /contact has a form capturing name, email, company, message, and selected plan"
    - "Visiting /contact?plan=agency pre-selects the Agency plan in the form"
    - "GET /sitemap.xml returns valid XML listing all site pages"
    - "GET /robots.txt returns a file allowing all crawlers with a Sitemap: reference"
    - "Every page has unique title, meta description, canonical URL, and Open Graph tags in HTML source"
    - "og-default.jpg exists at public/og-default.jpg for pages without custom OG images"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Contact page with form — 'use client' form wrapper"
      exports: ["default ContactPage", "metadata"]
    - path: "src/app/sitemap.ts"
      provides: "Dynamic Next.js sitemap listing all routes"
      exports: ["default sitemap function"]
    - path: "public/robots.txt"
      provides: "robots.txt allowing all crawlers with Sitemap reference"
    - path: "public/og-default.jpg"
      provides: "Default OG image for pages without custom images"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "all page routes"
      via: "hardcoded array of all static route paths in the site"
    - from: "public/robots.txt"
      to: "sitemap URL"
      via: "Sitemap: https://kineticrecruiter.com/sitemap.xml"
    - from: "src/lib/metadata.ts"
      to: "all page metadata exports"
      via: "generatePageMetadata called in every page.tsx — sets canonical, og:url, og:image"
---

<objective>
Build the contact page with a functional form, generate the dynamic sitemap covering all Phase 2 pages, create robots.txt, and ensure the default OG image exists. This completes all SEO infrastructure for the site.

Purpose: SEO infrastructure ensures every page is discoverable. The sitemap and robots.txt are crawled by Google within hours of deployment. Contact page captures Agency plan leads.
Output: /contact page, sitemap.ts (generates /sitemap.xml), robots.txt, og-default.jpg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From src/lib/metadata.ts (Phase 1) — already configured for every page -->
```typescript
// generatePageMetadata produces Metadata with:
// title: `${opts.title} | KineticRecruiter`
// metadataBase: new URL("https://kineticrecruiter.com")
// alternates.canonical: opts.path
// openGraph.url: opts.path
// openGraph.images: [opts.image ?? '/og-default.jpg']
// twitter.card: 'summary_large_image'
export function generatePageMetadata(opts: { title: string; description: string; path: string; image?: string }): Metadata
```

<!-- Next.js App Router sitemap.ts contract -->
```typescript
// src/app/sitemap.ts — returns MetadataRoute.Sitemap array
import { MetadataRoute } from 'next';
export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: 'https://kineticrecruiter.com/', lastModified: new Date(), changeFrequency: 'weekly', priority: 1.0 },
    // ... more routes
  ];
}
```

<!-- useSearchParams for ?plan= query param (contact form pre-fill) -->
// useSearchParams requires 'use client' and Suspense boundary
// Pattern: wrap the form component in Suspense with fallback in the server page
</interfaces>

<decisions-reference>
D-18: Contact form captures name, email, company, message, and selected plan (if referred via ?plan=agency)
D-19: Form submission can be a simple mailto or API endpoint — no backend processing required for v1
</decisions-reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build contact page with pre-fillable form</name>
  <files>
    src/app/contact/page.tsx
    src/components/contact/ContactForm.tsx
  </files>

  <read_first>
    - src/lib/metadata.ts — generatePageMetadata signature (already in interfaces above)
    - src/app/globals.css — Tailwind v4 CSS vars (use inline hex for brand colors)
    - .planning/phases/02-core-marketing-site/02-CONTEXT.md §D-18 and §D-19 — form fields and submission approach
  </read_first>

  <action>
    Create src/components/contact/ContactForm.tsx ('use client'):
    - 'use client' directive at top
    - import { useSearchParams } from 'next/navigation'
    - import { useState } from 'react'
    - Read ?plan= query param with useSearchParams() to pre-select plan in the select field
    - Form fields (all required except plan which has a default):
        name: text input, placeholder "Your name", required
        email: email input, placeholder "work@example.com", required
        company: text input, placeholder "Company name", required
        message: textarea rows={5}, placeholder "Tell us about your team and what you're looking for", required
        plan: select dropdown with options: General Inquiry, Starter, Professional, Agency — pre-selected from ?plan= param
    - Form state: useState for each field or a single formData object { name, email, company, message, plan }
    - Submit handler (per D-19 — simple mailto for v1):
        const subject = encodeURIComponent(`KineticRecruiter Contact - ${formData.plan}`)
        const body = encodeURIComponent(`Name: ${formData.name}\nEmail: ${formData.email}\nCompany: ${formData.company}\nPlan: ${formData.plan}\n\nMessage:\n${formData.message}`)
        window.location.href = `mailto:hello@kineticrecruiter.com?subject=${subject}&body=${body}`
        Show a submitted state with a success message: "Thanks! We'll be in touch within 1 business day."
    - Styling: form with space-y-6; inputs with border border-gray-300 rounded-lg px-4 py-3 w-full focus:outline-none focus:ring-2 focus:ring-[#0d8488]; submit button with bg-[#0d8488] text-white rounded-lg px-8 py-3 font-semibold hover:bg-[#0a6b6e] w-full

    Create src/app/contact/page.tsx (server component):
    - Export metadata: generatePageMetadata({ title: 'Contact Us', description: 'Get in touch with the KineticRecruiter team. Questions about plans, features, or getting started? We respond within 1 business day.', path: '/contact' })
    - Two-column layout on lg: left col (contact info + why contact), right col (form)
    - Left column content:
        h1: "Get in touch" (or equivalent — decide based on page purpose)
        Subtext: "For Agency plan inquiries, demos, or general questions — we typically respond within 1 business day."
        Contact email: hello@kineticrecruiter.com
        Optional: bullet points about what to expect
    - Right column: wrap ContactForm in Suspense (required because ContactForm uses useSearchParams):
        import { Suspense } from 'react'
        <Suspense fallback={<div>Loading form...</div>}>
          <ContactForm />
        </Suspense>
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      ls src/app/contact/page.tsx && \
      ls src/components/contact/ContactForm.tsx && \
      grep "'use client'" src/components/contact/ContactForm.tsx && \
      grep "useSearchParams" src/components/contact/ContactForm.tsx && \
      grep "Suspense" src/app/contact/page.tsx && \
      grep "generatePageMetadata" src/app/contact/page.tsx && \
      npm run build 2>&1 | grep -E "error TS|✓ Compiled" | head -5
    </automated>
  </verify>

  <acceptance_criteria>
    - src/components/contact/ContactForm.tsx exists with 'use client' and useSearchParams
    - grep "name\|email\|company\|message\|plan" src/components/contact/ContactForm.tsx shows all 5 fields
    - src/app/contact/page.tsx wraps ContactForm in Suspense
    - src/app/contact/page.tsx does NOT contain 'use client' (server component)
    - npm run build exits 0 — no "useSearchParams() should be wrapped in a suspense boundary" error
    - /contact route appears in build output
  </acceptance_criteria>

  <done>Contact page with 5-field form (pre-fills plan from ?plan= query param), mailto submission, Suspense boundary wrapping the client component, and unique SEO metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Create sitemap.ts, robots.txt, and default OG image</name>
  <files>
    src/app/sitemap.ts
    public/robots.txt
    public/og-default.jpg
  </files>

  <read_first>
    - src/lib/metadata.ts — confirm metadataBase URL is https://kineticrecruiter.com
    - package/kinetic-recruiter/public/ — check if any og-image or social preview image exists to copy
    - package/imgs/ — check for any suitable OG image (800x630 or 1200x630 preferred)
  </read_first>

  <action>
    Create src/app/sitemap.ts:
    - Import MetadataRoute from 'next'
    - Export default function sitemap(): MetadataRoute.Sitemap
    - Include all Phase 2 static routes with appropriate priority and changeFrequency:
      { url: 'https://kineticrecruiter.com/', lastModified: new Date(), changeFrequency: 'weekly', priority: 1.0 }
      { url: 'https://kineticrecruiter.com/pricing', lastModified: new Date(), changeFrequency: 'monthly', priority: 0.9 }
      { url: 'https://kineticrecruiter.com/features/ai-candidate-intelligence', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/features/candidate-intake', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/features/agency-workflow', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/features/team-platform', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/solutions/recruitment-agencies', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/solutions/in-house-teams', ..., priority: 0.8 }
      { url: 'https://kineticrecruiter.com/contact', ..., changeFrequency: 'yearly', priority: 0.6 }
    - NOTE: Blog posts are NOT in Phase 2 sitemap — they will be added dynamically in Phase 3 when blog.ts is used
    - changeFrequency types: 'always' | 'hourly' | 'daily' | 'weekly' | 'monthly' | 'yearly' | 'never'

    Create public/robots.txt:
    - File contents:
      ```
      User-agent: *
      Allow: /

      Sitemap: https://kineticrecruiter.com/sitemap.xml
      ```
    - No Disallow rules — all pages are public marketing content

    Create public/og-default.jpg:
    - Check package/kinetic-recruiter/public/ and package/imgs/ for an existing OG-suitable image
    - If a suitable image exists (800x630 or 1200x630 landscape), copy it:
      cp {source} public/og-default.jpg
    - If no suitable image exists, copy the best available photo from package/imgs/ as a placeholder:
      cp package/imgs/diverse-business-team-collaborating-modern-office.jpg public/og-default.jpg
    - NOTE: The og-default.jpg should be at least 600x300 pixels. If using a portrait image, it will still work as a fallback.
    - Document which file was used in the task completion comment.
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      grep "sitemap.xml" public/robots.txt && \
      grep "kineticrecruiter.com/pricing" src/app/sitemap.ts && \
      grep "solutions/recruitment-agencies" src/app/sitemap.ts && \
      ls public/og-default.jpg && \
      npm run build 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - public/robots.txt exists and contains "Sitemap: https://kineticrecruiter.com/sitemap.xml"
    - public/robots.txt contains "User-agent: *" and "Allow: /"
    - src/app/sitemap.ts exists and contains all 9 routes (homepage + 8 content pages)
    - grep -c "url:" src/app/sitemap.ts returns 9 (9 route entries)
    - public/og-default.jpg exists and is a non-empty file (ls -la shows size > 0)
    - npm run build exits 0
    - After npm run build, visiting /sitemap.xml in dev mode returns XML (run: curl http://localhost:3000/sitemap.xml | head -5 — should start with <?xml)
  </acceptance_criteria>

  <done>sitemap.ts lists all 9 Phase 2 routes; robots.txt allows all crawlers with sitemap reference; og-default.jpg placed in public/ as fallback OG image for all pages using generatePageMetadata().</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `npm run dev` and visit localhost:3000/contact — form renders with 5 fields
3. Visit localhost:3000/contact?plan=agency — Agency pre-selected in plan dropdown
4. curl http://localhost:3000/sitemap.xml — returns XML starting with <?xml, listing all 9 routes
5. curl http://localhost:3000/robots.txt — returns "User-agent: *" and "Sitemap:" line
6. view-source of any page — verify og:image tag pointing to /og-default.jpg in the HTML head
</verification>

<success_criteria>
- /contact renders form with name, email, company, message, plan fields; ?plan=agency pre-selects Agency
- /sitemap.xml returns valid XML listing homepage, pricing, 4 feature pages, 2 solution pages, contact
- /robots.txt allows all crawlers with Sitemap reference
- og-default.jpg exists in public/
- Every page in Phase 2 has unique title + description (via generatePageMetadata) — verifiable in build output
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-site/02-05-SUMMARY.md`
</output>
