---
phase: 02-core-marketing-site
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/FeatureGrid.tsx
  - src/components/sections/ComparisonStrip.tsx
  - src/components/sections/PricingPreview.tsx
  - src/components/sections/CTASection.tsx
  - src/components/sections/SocialProof.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Visiting / shows a full homepage: hero, feature grid, comparison strip, pricing preview, social proof, and bottom CTA"
    - "The hero section has a headline, subheadline, primary CTA ('Start Free Trial'), and a secondary CTA ('View Pricing')"
    - "The feature grid shows 6 feature cards with icons and descriptions"
    - "The comparison strip is a dark section showing $0/$0/$0 no hidden costs messaging"
    - "The pricing preview shows all 3 plan names and prices read from getAllPlans() — NOT hardcoded"
    - "The CTA section is a reusable component that can be dropped into any page"
    - "All section content matches the text and copy from MiniMax source components"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Homepage hero — server component"
      exports: ["default Hero"]
    - path: "src/components/sections/FeatureGrid.tsx"
      provides: "6-card feature grid — server component"
      exports: ["default FeatureGrid"]
    - path: "src/components/sections/ComparisonStrip.tsx"
      provides: "Dark $0/$0/$0 comparison section — server component"
      exports: ["default ComparisonStrip"]
    - path: "src/components/sections/PricingPreview.tsx"
      provides: "3-plan teaser reading from getAllPlans() — server component"
      exports: ["default PricingPreview"]
    - path: "src/components/sections/CTASection.tsx"
      provides: "Reusable bottom CTA section — server component"
      exports: ["default CTASection"]
    - path: "src/app/page.tsx"
      provides: "Homepage composing all sections with SSR metadata"
  key_links:
    - from: "src/app/page.tsx"
      to: "all section components"
      via: "import and render in order: Hero, FeatureGrid, ComparisonStrip, PricingPreview, SocialProof, CTASection"
    - from: "src/components/sections/PricingPreview.tsx"
      to: "src/lib/plans.ts"
      via: "import getAllPlans, call it server-side, pass plan data to render"
---

<objective>
Build all homepage section components and compose them into the homepage page.tsx. Every component is a server component. Content and copy are taken directly from MiniMax source files.

Purpose: Delivers the primary landing page with full SSR — every element visible in view-source for SEO crawlers. PricingPreview reads live plan data.
Output: Hero, FeatureGrid, ComparisonStrip, PricingPreview, CTASection, SocialProof components; homepage page.tsx composing them all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From src/lib/plans.ts (Phase 1) -->
```typescript
export type PlanKey = 'starter' | 'professional' | 'agency';
export interface Plan {
  name: string;
  tagline: string;
  monthly_price_cents: number;
  monthly_price_display: string;  // e.g. "$29"
  annual_price_cents: number;
  annual_price_display: string;   // e.g. "$24"
  annual_total_cents: number;
  annual_savings_percent: number;
  limits: { candidates: number | null; open_jobs: number | null; clients: number | null };
  features: string[];
  features_intro?: string;
  cta_text: string;
  cta_url: string;
  badge: string | null;
  sort_order: number;
}
export function getAllPlans(): Plan[]  // sorted by sort_order, returns [starter, professional, agency]
```

<!-- From src/lib/metadata.ts (Phase 1) -->
```typescript
export function generatePageMetadata(opts: {
  title: string;
  description: string;
  path: string;
  image?: string;
}): Metadata
```

<!-- Tailwind v4 brand color usage (globals.css @theme inline) -->
<!-- Use bg-[#0d8488] for kinetic-teal, bg-[#1a2332] for kinetic-navy -->
<!-- text-[#0d8488] for teal text, text-[#1a2332] for navy text -->
<!-- bg-[#E8F5F5] for teal-light background -->
<!-- Standard Tailwind utilities (gray-*, white, etc.) work normally -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero, FeatureGrid, ComparisonStrip sections</name>
  <files>
    src/components/sections/Hero.tsx
    src/components/sections/FeatureGrid.tsx
    src/components/sections/ComparisonStrip.tsx
    src/components/sections/.gitkeep
  </files>

  <read_first>
    - package/kinetic-recruiter/src/components/Hero.tsx — FULL FILE: headline text, subheadline, CTA labels, value proposition bullets, layout classes, browser mockup structure
    - package/kinetic-recruiter/src/components/Features.tsx — FULL FILE: all 6 feature card titles, descriptions, icon choices
    - package/kinetic-recruiter/src/components/Comparison.tsx — FULL FILE: comparison strip content, $0 messaging, column headers, dark background color
    - package/kinetic-recruiter/src/components/NoHiddenCosts.tsx — supplementary comparison content if present
  </read_first>

  <action>
    Create src/components/sections/Hero.tsx (server component — no 'use client'):
    - Read Hero.tsx from MiniMax for EXACT headline, subheadline, and bullet text
    - Layout: full-width section with gradient background bg-gradient-to-br from-[#E8F5F5] via-white to-white
    - Two-column grid on lg: left 5/12 (headline + CTA), right 7/12 (browser mockup illustration or recruiter image)
    - Headline: "The ATS that actually understands your candidates." (verify vs MiniMax source)
    - Primary CTA: "Start Free Trial" href="https://app.kineticrecruiter.com/register" — teal button
    - Secondary CTA: "See Pricing" href="/pricing" — outline button
    - Value prop bullets: 3-4 bullet points from MiniMax source (copy exactly)
    - Badge/label above headline: "Built for recruitment agencies" with pulse dot
    - Right side: use next/image with src="/images/recruiter-success.png" or similar — width={600} height={500} alt="KineticRecruiter ATS dashboard" priority={true}
    - If MiniMax uses an inline SVG browser mockup: recreate the same structure in HTML/JSX

    Create src/components/sections/FeatureGrid.tsx (server component):
    - Read Features.tsx from MiniMax for all 6 feature titles and descriptions
    - Section heading from MiniMax (e.g., "Everything you need to run your recruitment operation")
    - 3-column grid on md+: grid md:grid-cols-2 lg:grid-cols-3 gap-8
    - Each card: icon (lucide-react), bold title, description paragraph
    - 6 features from MiniMax (exact text): AI Candidate Discovery, AI Scoring, Career Highlights, Client CRM, Job Shortlist, Chrome Extension (verify exact names from source)
    - Background: bg-[#F9FAFB] or bg-gray-50 for section background

    Create src/components/sections/ComparisonStrip.tsx (server component):
    - Read Comparison.tsx and NoHiddenCosts.tsx from MiniMax for exact content
    - Dark background section: bg-[#1a2332]
    - Heading, subheading from MiniMax source (copy exactly)
    - Three columns showing $0 for setup fees, $0 for per-seat AI charges, $0 for integration fees
    - Or a comparison table if MiniMax uses table format — copy the structure
    - Text: white/gray-300 on dark background
    - All section content is server-rendered static HTML — no client state needed
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      grep -l "next/image" src/components/sections/Hero.tsx && \
      grep -l "getAllPlans\|Features\|FeatureGrid" src/components/sections/FeatureGrid.tsx || true && \
      grep -l "#1a2332\|kinetic-navy" src/components/sections/ComparisonStrip.tsx || true && \
      npm run build 2>&1 | grep -E "error|Error|✓ Compiled" | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - src/components/sections/Hero.tsx exists, does NOT contain 'use client', uses next/image
    - src/components/sections/FeatureGrid.tsx exists, does NOT contain 'use client', contains 6 feature entries (grep shows at least 6 feature title strings)
    - src/components/sections/ComparisonStrip.tsx exists, does NOT contain 'use client', contains dark background color (#1a2332 or navy)
    - npm run build passes with 0 TypeScript errors
    - All headline and copy text matches MiniMax source (verifiable by reading the files and comparing)
  </acceptance_criteria>

  <done>Hero, FeatureGrid, ComparisonStrip created as server components with exact MiniMax copy text, correct layouts, and next/image usage.</done>
</task>

<task type="auto">
  <name>Task 2: Build PricingPreview, CTASection, SocialProof and compose homepage</name>
  <files>
    src/components/sections/PricingPreview.tsx
    src/components/sections/CTASection.tsx
    src/components/sections/SocialProof.tsx
    src/app/page.tsx
  </files>

  <read_first>
    - package/kinetic-recruiter/src/components/PricingPreview.tsx — FULL FILE: plan card layout, price display, CTA labels, styling
    - package/kinetic-recruiter/src/components/CTASection.tsx — FULL FILE: headline, subheadline, CTA button text and URLs, background style
    - package/kinetic-recruiter/src/components/SocialProof.tsx — FULL FILE: testimonial/stats content, logos if any
    - package/kinetic-recruiter/src/components/Testimonials.tsx — FULL FILE: testimonial cards, names, companies, quotes
    - src/lib/plans.ts — getAllPlans() return type (already read in interfaces block above)
  </read_first>

  <action>
    Create src/components/sections/PricingPreview.tsx (server component):
    - Call getAllPlans() at the top of the component (server-side — no fetch needed, it reads JSON directly)
    - Show all 3 plans in a 3-column grid
    - Each plan card shows: plan name, tagline, monthly price display (monthly_price_display), "/ month" label, badge if plan.badge is non-null
    - "View All Plans" or "Start Free Trial" CTA linking to /pricing
    - Professional plan card gets a visual highlight (border-[#0d8488] or scale transform)
    - No toggle needed here — pricing preview shows monthly prices only; full toggle is on /pricing page
    - Section heading from MiniMax PricingPreview source (copy exactly)

    Create src/components/sections/CTASection.tsx (server component):
    - Reusable component — accepts optional props: headline?: string, subheadline?: string, primaryCTA?: {text: string, href: string}, secondaryCTA?: {text: string, href: string}
    - Defaults from MiniMax CTASection.tsx (copy exact default text — verify from source)
    - Background: teal gradient or navy (match MiniMax)
    - Two buttons: primary (white bg on dark section), secondary (outline on dark section)
    - This component is REUSED on every feature page and solution page

    Create src/components/sections/SocialProof.tsx (server component):
    - Read SocialProof.tsx and Testimonials.tsx from MiniMax
    - If MiniMax has testimonial cards: recreate with same quotes, names, companies, roles
    - If MiniMax has a stats bar (e.g., "500+ agencies", "10,000+ placements"): recreate exactly
    - All static data hardcoded as constants in the component (server component, no fetch)
    - next/image for any avatar or logo images (with alt text)

    Update src/app/page.tsx (REPLACE the Phase 1 placeholder):
    - Server component, no 'use client'
    - Export metadata using generatePageMetadata():
      title: "KineticRecruiter — The ATS That Understands Your Candidates"
      description: "AI-powered applicant tracking system for recruitment agencies. Semantic search, candidate scoring, and client CRM. Start your free trial today."
      path: "/"
    - Import and render sections in order: Hero, FeatureGrid, ComparisonStrip, PricingPreview, SocialProof, CTASection
    - Wrap in a main element: <main className="flex flex-col">
  </action>

  <verify>
    <automated>
      cd /Users/admin/Documents/Development/kineticrecruiter-site && \
      grep "getAllPlans" src/components/sections/PricingPreview.tsx && \
      grep "generatePageMetadata" src/app/page.tsx && \
      npm run build 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "getAllPlans" src/components/sections/PricingPreview.tsx returns a match (pricing from plans.json, not hardcoded)
    - grep "generatePageMetadata" src/app/page.tsx returns a match (SEO metadata)
    - grep "'use client'" src/components/sections/PricingPreview.tsx returns no match (server component)
    - grep "'use client'" src/components/sections/CTASection.tsx returns no match (server component)
    - src/app/page.tsx imports Hero, FeatureGrid, ComparisonStrip, PricingPreview, SocialProof, CTASection
    - npm run build exits 0
    - curl http://localhost:3000 (after npm run dev) shows non-empty HTML body with hero headline text
  </acceptance_criteria>

  <done>PricingPreview (reads getAllPlans() server-side), CTASection (reusable with defaults), SocialProof assembled and composed into homepage page.tsx with full SSR metadata.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `npm run dev` — visit localhost:3000, view-source shows full HTML including hero headline text (not blank)
3. Check pricing preview section — verify plan names/prices visible in view-source (not blank)
4. CTASection renders at bottom of page
</verification>

<success_criteria>
- Homepage renders full HTML server-side — hero, feature grid, comparison strip, pricing preview, social proof, CTA all visible in view-source
- PricingPreview reads from getAllPlans() — no hardcoded plan names or prices
- CTASection component is reusable with sensible defaults for use on other pages
- All copy matches MiniMax source text
- npm run build passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-marketing-site/02-02-SUMMARY.md`
</output>
