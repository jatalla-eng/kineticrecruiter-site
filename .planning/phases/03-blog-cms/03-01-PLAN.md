---
phase: 03-blog-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/blog/page.tsx
  - src/app/blog/[slug]/page.tsx
  - src/components/blog/BlogCard.tsx
  - src/components/blog/BlogIndex.tsx
  - src/app/sitemap.ts
  - content/blog/best-ats-for-recruitment-agencies-2026.md
  - content/blog/ai-changing-recruitment-agencies-2026.md
  - public/images/blog/.gitkeep
autonomous: true
requirements: [BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05, BLOG-06, BLOG-07]

must_haves:
  truths:
    - "Visiting /blog lists all posts with title, date, category badge, description, and reading time"
    - "Category filter pills narrow the list without a page reload"
    - "Each post at /blog/[slug] renders markdown as styled HTML with prose typography"
    - "Post pages show title, date, category badge, reading time, featured image, prose content, author attribution"
    - "Article JSON-LD is present in the HTML source of each post page"
    - "npm run build completes without errors and produces static HTML for each post"
    - "/blog and /blog/[slug] entries appear in the sitemap output"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "Blog index server component"
    - path: "src/components/blog/BlogIndex.tsx"
      provides: "Client-side category filter with BlogCard grid"
    - path: "src/components/blog/BlogCard.tsx"
      provides: "Blog post card UI"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Static post page with generateStaticParams, JSON-LD, generateMetadata"
    - path: "content/blog/best-ats-for-recruitment-agencies-2026.md"
      provides: "Seed post — Comparisons category"
    - path: "content/blog/ai-changing-recruitment-agencies-2026.md"
      provides: "Seed post — AI in Recruitment category"
    - path: "src/app/sitemap.ts"
      provides: "Extended sitemap with /blog and post slugs"
  key_links:
    - from: "src/app/blog/page.tsx"
      to: "src/components/blog/BlogIndex.tsx"
      via: "passes getAllPosts() result as initialPosts prop"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/lib/blog.ts"
      via: "calls getPostBySlug(params.slug) and getAllSlugs() for generateStaticParams"
    - from: "src/components/blog/BlogIndex.tsx"
      to: "src/components/blog/BlogCard.tsx"
      via: "maps filtered posts to BlogCard components"
    - from: "src/app/sitemap.ts"
      to: "src/lib/blog.ts"
      via: "calls getAllPosts() to generate blog post URLs"
---

<objective>
Build the complete blog UI layer on top of the Phase 1 blog.ts infrastructure. This plan creates the blog index page with client-side category filtering, individual post pages with prose typography and Article JSON-LD, two real seed blog posts, and extends the sitemap with blog routes.

Purpose: Enable KineticRecruiter to publish SEO-optimized blog content that search engines can discover and rank.
Output: /blog listing page, /blog/[slug] post pages, 2 seed posts, sitemap extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-blog-cms/03-CONTEXT.md
@.planning/phases/03-blog-cms/03-RESEARCH.md

<interfaces>
<!-- Existing interfaces from Phase 1. Use these directly — no codebase exploration needed. -->

From src/lib/blog.ts:
```typescript
export interface BlogPost {
  slug: string;
  title: string;
  date: string;
  category: string;
  description: string;
  image: string;
  author: string;
  readingTime: string;
  content?: string;  // Only present when returned from getPostBySlug()
}

export function getAllPosts(): BlogPost[]
export async function getPostBySlug(slug: string): Promise<BlogPost | null>
export function getAllSlugs(): string[]
```

From src/components/ui/Badge.tsx:
Use this for category badges. Check the file for exact props interface before use.

From src/components/sections/CTASection.tsx:
Use this at the bottom of blog index and post pages. Check the file for exact props.

From src/app/globals.css:
```css
@import "tailwindcss";
/* Add @plugin "@tailwindcss/typography"; on a new line after the import */
```

Brand color token: `--color-kinetic-teal: #0d8488` (use as `text-kinetic-teal`, `bg-kinetic-teal`, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install typography plugin, create blog components, and seed posts</name>
  <files>
    src/app/globals.css,
    src/components/blog/BlogCard.tsx,
    src/components/blog/BlogIndex.tsx,
    content/blog/best-ats-for-recruitment-agencies-2026.md,
    content/blog/ai-changing-recruitment-agencies-2026.md,
    public/images/blog/.gitkeep
  </files>
  <read_first>
    - src/app/globals.css — to add @plugin line after existing @import
    - src/components/ui/Badge.tsx — to understand exact component API before use in BlogCard
    - src/components/sections/CTASection.tsx — to understand props before use in BlogIndex
    - package.json — to verify @tailwindcss/typography is not already installed
  </read_first>
  <action>
**Step 1: Install typography plugin (per D-01)**

Run:
```
npm install @tailwindcss/typography@latest
```

In src/app/globals.css, add this line immediately after `@import "tailwindcss";`:
```css
@plugin "@tailwindcss/typography";
```

**Step 2: Create content directory and seed posts (per D-05, D-06)**

Create directory: `content/blog/`

Create `content/blog/best-ats-for-recruitment-agencies-2026.md`:
- Frontmatter: title, date (2026-03-15), category (Comparisons), description, image (/images/blog/best-ats-comparison.jpg), author (KineticRecruiter Team)
- Title: "Best ATS for Recruitment Agencies in 2026: KineticRecruiter vs Greenhouse vs Lever"
- Description: A detailed comparison of the top ATS platforms for agency recruiters, covering pricing, features, and ease of use.
- Content: Write a real, SEO-optimized 800-1000 word article. Include H2 sections: "Why Recruitment Agencies Need a Specialist ATS", "The Contenders: How We Evaluated", compare Greenhouse (enterprise-focused, expensive at $6k+/yr, complex onboarding), Lever (mid-market, CRM features, limited agency-specific tools), KineticRecruiter (agency-first, flat pricing from $89/mo, AI screening, candidate intake portals). Include a comparison table. End with a "Bottom Line" section. Write from the perspective of an agency operator. Use factual-sounding but reasonable pricing/feature claims.

Create `content/blog/ai-changing-recruitment-agencies-2026.md`:
- Frontmatter: title, date (2026-02-20), category (AI in Recruitment), description, image (/images/blog/ai-recruitment.jpg), author (KineticRecruiter Team)
- Title: "How AI Is Transforming Recruitment Agencies in 2026"
- Description: Explore how artificial intelligence is reshaping the recruitment industry, from automated screening to predictive candidate matching.
- Content: Write a real, SEO-optimized 800-1000 word article. Include H2 sections: "The Screening Bottleneck AI Solves", "Beyond Keyword Matching: Semantic Candidate Intelligence", "AI-Powered Candidate Intake Portals", "What Agencies Are Gaining in Practice" (concrete benefits: 60% reduction in screening time, faster placement cycles), "Getting Started Without Disrupting Your Workflow". Write from the perspective of an agency growth consultant. Make claims specific and actionable.

Create `public/images/blog/.gitkeep` (empty file to ensure the directory is tracked in git so Decap CMS can write images here).

**Step 3: Create BlogCard component**

Create `src/components/blog/BlogCard.tsx`:
- Props: `{ post: BlogPost }` (import BlogPost from @/lib/blog)
- Render as a `<Link href="/blog/{post.slug}">` wrapping a card div
- Card layout: featured image (next/image, aspect-ratio 16/9, object-cover), then content area with category Badge, title (text-xl font-semibold), description (text-sm text-gray-600, line-clamp-2), and a metadata row showing formatted date + reading time
- Use `<Badge>` from @/components/ui/Badge for the category
- Style: bg-white rounded-xl overflow-hidden border border-gray-100 hover:shadow-md transition-shadow
- For the image: use next/image with `fill` or fixed dimensions (400x225); if post.image is missing fall back to a light gray placeholder bg

**Step 4: Create BlogIndex client component (per D-04)**

Create `src/components/blog/BlogIndex.tsx`:
- Mark `'use client'` at top
- Props: `{ initialPosts: BlogPost[] }` (import BlogPost type)
- Define `const CATEGORIES = ['All', 'Comparisons', 'AI in Recruitment', 'Agency Growth', 'Product Updates', 'Guides']` (per D-03)
- Use `useState<string>('All')` for selectedCategory
- Filter logic: `selectedCategory === 'All' ? initialPosts : initialPosts.filter(p => p.category === selectedCategory)`
- Render category pills: a row of `<button>` elements, active pill uses `bg-kinetic-teal text-white`, inactive uses `bg-gray-100 text-gray-700`, all pills: `px-4 py-2 rounded-full text-sm font-medium transition-colors`
- Render BlogCard grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- If filtered list is empty, show a friendly "No posts in this category yet." message
- Import CTASection and render it below the grid
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@plugin" src/app/globals.css` outputs `@plugin "@tailwindcss/typography";`
    - `ls content/blog/*.md | wc -l` outputs `2`
    - `grep "category:" content/blog/best-ats-for-recruitment-agencies-2026.md` outputs `category: Comparisons`
    - `grep "category:" content/blog/ai-changing-recruitment-agencies-2026.md` outputs `category: AI in Recruitment`
    - `ls src/components/blog/` shows `BlogCard.tsx` and `BlogIndex.tsx`
    - `grep "'use client'" src/components/blog/BlogIndex.tsx` matches
    - `grep "CATEGORIES" src/components/blog/BlogIndex.tsx` matches
    - `ls public/images/blog/.gitkeep` exists
  </acceptance_criteria>
  <done>
    Typography plugin installed and registered. Two seed posts exist with correct frontmatter. BlogCard and BlogIndex components compile without TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Blog index page, post page with JSON-LD, and sitemap extension</name>
  <files>
    src/app/blog/page.tsx,
    src/app/blog/[slug]/page.tsx,
    src/app/sitemap.ts
  </files>
  <read_first>
    - src/lib/blog.ts — confirm BlogPost interface and function signatures (already loaded above)
    - src/app/sitemap.ts — read current content before extending
    - src/lib/metadata.ts — to understand generatePageMetadata helper if it exists
  </read_first>
  <action>
**Step 1: Create blog index route**

Create `src/app/blog/page.tsx` (server component, no 'use client'):
```typescript
import { getAllPosts } from '@/lib/blog';
import BlogIndex from '@/components/blog/BlogIndex';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Blog | KineticRecruiter',
  description: 'Recruitment insights, ATS guides, and agency growth strategies from the KineticRecruiter team.',
  openGraph: {
    title: 'Blog | KineticRecruiter',
    description: 'Recruitment insights, ATS guides, and agency growth strategies.',
    type: 'website',
  },
};

export default function BlogPage() {
  const posts = getAllPosts();
  return (
    <main>
      <section className="bg-kinetic-navy text-white py-16 px-4">
        <div className="max-w-6xl mx-auto">
          <h1 className="text-4xl md:text-5xl font-bold mb-4">Blog</h1>
          <p className="text-xl text-gray-300 max-w-2xl">
            Recruitment insights, ATS comparisons, and agency growth strategies.
          </p>
        </div>
      </section>
      <section className="max-w-6xl mx-auto px-4 py-12">
        <BlogIndex initialPosts={posts} />
      </section>
    </main>
  );
}
```

Note: getAllPosts() is synchronous (no await needed). Do not add 'async' to the component.

**Step 2: Create blog post dynamic route (per D-09, D-10, D-11, BLOG-07)**

Create directory: `src/app/blog/[slug]/`

Create `src/app/blog/[slug]/page.tsx`:

```typescript
import { getPostBySlug, getAllSlugs } from '@/lib/blog';
import { notFound } from 'next/navigation';
import Image from 'next/image';
import Link from 'next/link';
import Badge from '@/components/ui/Badge';
import CTASection from '@/components/sections/CTASection';
import type { Metadata } from 'next';

// Build-time static generation — called once during npm run build
export async function generateStaticParams() {
  const slugs = getAllSlugs();
  return slugs.map((slug) => ({ slug }));
}

type Props = { params: Promise<{ slug: string }> };

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { slug } = await params;
  const post = await getPostBySlug(slug);
  if (!post) return { title: 'Post Not Found' };
  return {
    title: `${post.title} | KineticRecruiter Blog`,
    description: post.description,
    openGraph: {
      type: 'article',
      title: post.title,
      description: post.description,
      images: post.image ? [{ url: post.image }] : [],
      publishedTime: post.date,
      authors: [post.author],
    },
  };
}

export default async function BlogPostPage({ params }: Props) {
  const { slug } = await params;
  const post = await getPostBySlug(slug);
  if (!post) notFound();

  // Article JSON-LD structured data (per D-10, BLOG-06)
  const articleSchema = {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: post.title,
    description: post.description,
    image: post.image,
    datePublished: post.date,
    dateModified: post.date,
    author: {
      '@type': 'Organization',
      name: post.author,
    },
    publisher: {
      '@type': 'Organization',
      name: 'KineticRecruiter',
      url: 'https://kineticrecruiter.com',
    },
  };

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(articleSchema) }}
      />
      <main>
        <article className="max-w-3xl mx-auto px-4 py-12">
          {/* Header: title, metadata row, featured image (per D-02) */}
          <header className="mb-8">
            <Link href="/blog" className="text-kinetic-teal text-sm font-medium hover:underline mb-6 inline-block">
              ← Back to Blog
            </Link>
            <h1 className="text-3xl md:text-4xl font-bold text-gray-900 mb-4">{post.title}</h1>
            <div className="flex flex-wrap items-center gap-3 text-sm text-gray-600 mb-6">
              <time dateTime={post.date}>
                {new Date(post.date).toLocaleDateString('en-AU', {
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric',
                })}
              </time>
              <span aria-hidden>·</span>
              <Badge>{post.category}</Badge>
              <span aria-hidden>·</span>
              <span>{post.readingTime}</span>
            </div>
            {post.image && (
              <div className="relative w-full h-64 md:h-96 rounded-xl overflow-hidden mb-8">
                <Image
                  src={post.image}
                  alt={post.title}
                  fill
                  className="object-cover"
                  priority
                />
              </div>
            )}
          </header>

          {/* Prose content — typography plugin applies styles (per D-01, BLOG-03) */}
          <div
            className="prose prose-lg max-w-none prose-headings:text-gray-900 prose-a:text-kinetic-teal prose-a:no-underline hover:prose-a:underline prose-code:bg-gray-100 prose-code:px-1 prose-code:rounded"
            dangerouslySetInnerHTML={{ __html: post.content! }}
          />

          {/* Author attribution (per D-02) */}
          <footer className="mt-12 pt-8 border-t border-gray-200">
            <p className="text-gray-600">
              Written by <strong className="text-gray-900">{post.author}</strong>
            </p>
          </footer>
        </article>

        <CTASection />
      </main>
    </>
  );
}
```

**Step 3: Extend sitemap.ts with blog routes (per D-16)**

Read the current src/app/sitemap.ts first. Convert the function to async and add blog routes:

- Change `export default function sitemap()` to `export default async function sitemap()`
- Import `getAllPosts` from `@/lib/blog`
- At the top of the function body call `const posts = getAllPosts();`
- Build `const blogPostRoutes` array from posts: each entry `{ url: BASE_URL + '/blog/' + post.slug, lastModified: new Date(post.date), changeFrequency: 'monthly' as const, priority: 0.7 }`
- Add a `/blog` index entry: `{ url: BASE_URL + '/blog', lastModified: new Date(), changeFrequency: 'weekly' as const, priority: 0.8 }`
- Spread `blogPostRoutes` into the returned array AFTER the existing routes
- The `/blog` index entry goes between the `/contact` entry and the spread blog post routes
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error|✓|Route)" | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/app/blog/page.tsx` exists
    - `ls src/app/blog/[slug]/page.tsx` exists
    - `grep "generateStaticParams" src/app/blog/\[slug\]/page.tsx` matches
    - `grep "application/ld+json" src/app/blog/\[slug\]/page.tsx` matches
    - `grep "BlogPosting" src/app/blog/\[slug\]/page.tsx` matches
    - `grep "getAllPosts" src/app/sitemap.ts` matches
    - `grep "/blog" src/app/sitemap.ts` matches
    - `npm run build` exits with code 0 (no TypeScript or build errors)
    - Build output lists `/blog` and `/blog/[slug]` as generated routes
  </acceptance_criteria>
  <done>
    Blog index page renders server-side with category filter. Each post page is statically generated with Article JSON-LD in source. Sitemap includes /blog and all post slugs. `npm run build` completes without errors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full blog system:

1. `npm run build` — must complete without errors; build output should show:
   - Route `/blog` (SSG or SSR)
   - Route `/blog/best-ats-for-recruitment-agencies-2026` (SSG)
   - Route `/blog/ai-changing-recruitment-agencies-2026` (SSG)

2. `npm run dev` and visit http://localhost:3000/blog — category pills should appear, both posts should be visible

3. Click a category pill — list filters without page reload

4. Visit http://localhost:3000/blog/best-ats-for-recruitment-agencies-2026 — post renders with styled prose, reading time, category badge

5. View source of that page — search for `"BlogPosting"` — should appear in a JSON-LD script tag

6. `curl http://localhost:3000/sitemap.xml | grep blog` — should list /blog and both post URLs
</verification>

<success_criteria>
- All 7 BLOG requirements (BLOG-01 through BLOG-07) satisfied
- `npm run build` exits 0 with static HTML generated for each post
- Blog index shows posts with category filter (client-side, no reload)
- Post pages have Article JSON-LD in source HTML
- Sitemap includes /blog and all post slugs
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-blog-cms/03-01-SUMMARY.md` with:
- What was built (components, pages, seed posts)
- Files created/modified
- Key decisions made (typography plugin syntax used, prose classes applied)
- Any deviations from the plan
- Verification commands that passed
</output>
