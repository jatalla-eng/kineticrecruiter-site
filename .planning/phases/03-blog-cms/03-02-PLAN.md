---
phase: 03-blog-cms
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/admin/index.html
  - public/admin/config.yml
autonomous: true
requirements: [CMS-01, CMS-02, CMS-03, CMS-04]

must_haves:
  truths:
    - "Visiting /admin in a browser loads the Decap CMS interface (no 404)"
    - "The CMS shows a GitHub login button and can authenticate via Netlify OAuth proxy"
    - "Authenticated editors can create new blog posts via the WYSIWYG editor"
    - "Editors can upload images via drag-and-drop into the image field"
    - "Saving a post commits a markdown file to content/blog/ in the GitHub repo"
  artifacts:
    - path: "public/admin/index.html"
      provides: "Decap CMS HTML shell loading decap-cms@^3.0.0 from unpkg CDN"
    - path: "public/admin/config.yml"
      provides: "Decap CMS backend, media, and collections configuration"
  key_links:
    - from: "public/admin/index.html"
      to: "https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"
      via: "<script src> tag loads CMS from CDN"
    - from: "public/admin/config.yml"
      to: "https://api.netlify.com"
      via: "base_url for GitHub OAuth proxy"
    - from: "public/admin/config.yml"
      to: "content/blog/"
      via: "collections[0].folder maps CMS saves to content/blog/*.md"
    - from: "public/admin/config.yml"
      to: "public/images/blog"
      via: "media_folder for uploaded images"
---

<objective>
Set up Decap CMS at /admin so non-developers (including Amy) can create and publish blog posts through a visual WYSIWYG editor. The CMS uses GitHub as the backend — saving a post commits a markdown file to the repo, triggering a rebuild.

Purpose: Enable non-developer content publishing without touching code or git CLI.
Output: public/admin/index.html + public/admin/config.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-blog-cms/03-CONTEXT.md
@.planning/phases/03-blog-cms/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Decap CMS admin files</name>
  <files>
    public/admin/index.html,
    public/admin/config.yml
  </files>
  <read_first>
    - .planning/phases/03-blog-cms/03-CONTEXT.md — decisions D-12 through D-15 define exact CMS config values
    - .planning/phases/03-blog-cms/03-RESEARCH.md — "Pattern 4: Decap CMS Configuration" shows exact config.yml structure
  </read_first>
  <action>
**Step 1: Create public/admin/ directory and index.html (per D-12)**

Create `public/admin/index.html`:

```html
<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>KineticRecruiter CMS</title>
  <script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
</head>
<body>
  <!-- Decap CMS auto-initializes from config.yml in the same directory -->
</body>
</html>
```

No other content needed. Decap CMS bootstraps itself entirely from the script tag and config.yml.

**Step 2: Create public/admin/config.yml (per D-13, D-14, D-15)**

Create `public/admin/config.yml`:

```yaml
backend:
  name: github
  repo: yajean/kineticrecruiter-site
  branch: main
  base_url: https://api.netlify.com
  auth_endpoint: auth

media_folder: public/images/blog
public_folder: /images/blog

collections:
  - name: blog
    label: Blog Posts
    label_singular: Blog Post
    folder: content/blog
    create: true
    slug: "{{slug}}"
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false }
      - label: Category
        name: category
        widget: select
        options:
          - Comparisons
          - AI in Recruitment
          - Agency Growth
          - Product Updates
          - Guides
      - { label: Description, name: description, widget: string, hint: "Short summary shown on blog index and in search results (150-160 characters ideal)" }
      - { label: Featured Image, name: image, widget: image, required: false, hint: "Recommended: 1200x630px JPG or PNG" }
      - { label: Author, name: author, widget: string, default: "KineticRecruiter Team" }
      - { label: Body, name: body, widget: markdown }
```

Important notes for config.yml:
- `repo` must match the actual GitHub org/repo. Use `yajean/kineticrecruiter-site` based on the git config. If that's wrong, the executor should run `git remote get-url origin` to confirm the exact repo path and use that value.
- `branch: main` — commits go to main, triggering rebuild on the deployment platform
- `media_folder: public/images/blog` — filesystem path where uploaded images are saved
- `public_folder: /images/blog` — URL path Decap inserts into markdown image references
- `date_format: "YYYY-MM-DD"` with `time_format: false` — stores date as ISO date string (e.g. 2026-04-10), matching the frontmatter format expected by blog.ts
- The `body` field name must be `body` (not `content`) — Decap CMS convention for the main markdown field

**Step 3: Verify GitHub repo name**

Run: `git remote get-url origin`

If the output is not `https://github.com/yajean/kineticrecruiter-site.git` or `git@github.com:yajean/kineticrecruiter-site.git`, update the `repo:` field in config.yml to match `{org}/{repo}` from the actual remote URL.
  </action>
  <verify>
    <automated>ls public/admin/index.html public/admin/config.yml && grep "decap-cms" public/admin/index.html && grep "github" public/admin/config.yml && grep "content/blog" public/admin/config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/admin/index.html` exists
    - `ls public/admin/config.yml` exists
    - `grep "unpkg.com/decap-cms" public/admin/index.html` matches
    - `grep "base_url: https://api.netlify.com" public/admin/config.yml` matches
    - `grep "media_folder: public/images/blog" public/admin/config.yml` matches
    - `grep "public_folder: /images/blog" public/admin/config.yml` matches
    - `grep "folder: content/blog" public/admin/config.yml` matches
    - `grep "widget: markdown" public/admin/config.yml` matches (body field)
    - `grep "Comparisons" public/admin/config.yml` matches (select options present)
    - `npm run build` exits with code 0 (public/admin files are static, no build impact)
  </acceptance_criteria>
  <done>
    /admin loads Decap CMS from CDN. config.yml configures GitHub backend with Netlify OAuth proxy, blog collection with all required fields, and media upload to public/images/blog.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. `npm run build` — should still complete without errors (public/ files are served as-is, not compiled)

2. `npm run dev` and visit http://localhost:3000/admin — Decap CMS interface should load (may show a login screen or error about OAuth — this is expected in local dev since Netlify OAuth proxy only works on deployed domains)

3. Check browser console at /admin — should show Decap CMS initializing, not a 404 or script error

4. Confirm config.yml is valid YAML: `cat public/admin/config.yml | python3 -c "import sys,yaml; yaml.safe_load(sys.stdin); print('YAML valid')"` — must print "YAML valid"

Note: Full OAuth login testing requires the site to be deployed to a domain registered with GitHub OAuth. In local dev, /admin will load the UI but login will fail — this is expected and not a blocker.
</verification>

<success_criteria>
- All 4 CMS requirements (CMS-01 through CMS-04) satisfied
- /admin loads Decap CMS HTML (200, not 404)
- config.yml is valid YAML with correct GitHub backend, media config, and blog collection
- Blog collection includes all fields: title, date, category (select), description, image, author, body (markdown)
- Media folder points to public/images/blog with /images/blog as public path
</success_criteria>

<output>
After completion, create `.planning/phases/03-blog-cms/03-02-SUMMARY.md` with:
- Files created
- Actual GitHub repo value used in config.yml
- Any adjustments made to the config (e.g. repo name correction)
- Verification commands that passed
</output>
